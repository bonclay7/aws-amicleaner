env:
  global:
    - AWS_DEFAULT_REGION=eu-west-1

dist: trusty
language: python
python:
  - "3.7"
  - "3.8"
  - "3.9"

install:
  - pip install -r requirements_build.txt

script:
  - py.test -v

after_success:
  - codecov
